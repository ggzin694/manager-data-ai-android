# Estrutura do projeto

Este pacote contém a base inicial da Manager Data AI:

- `backend/`: FastAPI modular com Manager, agentes, memória, provedores, segurança, custos, logs e migrations.
- `android/`: módulo Android do aplicativo cliente.

A chave da IA deve ficar somente no servidor, nunca no APK.
